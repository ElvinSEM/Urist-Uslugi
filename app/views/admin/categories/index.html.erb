<div class="mb-6 flex items-center justify-between">
  <h1 class="text-3xl font-semibold">Categories</h1>
  <%= link_to "New", new_admin_category_path, class: "rounded-lg bg-amber-500 px-4 py-2 text-black" %>
</div>
<% @categories.each do |category| %>
  <div class="mb-3 rounded-xl bg-slate-900 p-4"><%= category.name %></div>
<% end %>
